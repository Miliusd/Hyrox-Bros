"use client";
import { useState } from "react";
import { createBrowserClient } from "@/lib/supabase/client";
export default function LoginPage() { const [email, setEmail] = useState(""); const [message, setMessage] = useState(""); async function submit(e: React.FormEvent) { e.preventDefault(); try { const supabase = createBrowserClient(); const site = process.env.NEXT_PUBLIC_SITE_URL || window.location.origin; const { error } = await supabase.auth.signInWithOtp({ email, options: { emailRedirectTo: `${site}/auth/callback` } }); setMessage(error ? error.message : "Check your inbox for your sign-in link."); } catch (error) { setMessage(error instanceof Error ? error.message : "Could not send link"); } } return <main className="grid min-h-screen place-items-center px-4"><div className="w-full max-w-md"><div className="mb-8 text-center"><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-brand-400 text-2xl font-black text-ink-950">HB</div><h1 className="mt-5 text-4xl font-black">HYROX<span className="text-brand-400">BROS</span></h1><p className="mt-2 text-ink-400">Five athletes. One plan. No excuses.</p></div><form className="card space-y-4" onSubmit={submit}><label><span className="label">Your allowlisted email</span><input className="input" type="email" required value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@example.com" /></label><button className="btn-primary w-full">Send magic link</button>{message && <p className="text-sm text-ink-200">{message}</p>}</form></div></main>; }
