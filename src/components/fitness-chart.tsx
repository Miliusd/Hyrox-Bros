"use client";
import { Area, Bar, CartesianGrid, ComposedChart, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { FitnessPoint } from "@/lib/load";
export function FitnessChart({ data }: { data: FitnessPoint[] }) { const latest = data.at(-1); return <div className="h-80 w-full" role="img" aria-label="Training chart showing daily load, fitness, fatigue, and form over time"><span className="sr-only">Latest values: fitness {Math.round(latest?.fitness ?? 0)}, fatigue {Math.round(latest?.fatigue ?? 0)}, form {Math.round(latest?.form ?? 0)}, daily load {Math.round(latest?.load ?? 0)}.</span><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{ top: 8, right: 4, bottom: 0, left: -24 }}><defs><linearGradient id="fitness" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#ffd23f" stopOpacity={0.35} /><stop offset="1" stopColor="#ffd23f" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="#262c38" vertical={false} /><XAxis dataKey="date" tick={{ fill: "#929db1", fontSize: 12 }} tickFormatter={(value) => String(value).slice(5)} minTickGap={24} /><YAxis tick={{ fill: "#929db1", fontSize: 12 }} /><Tooltip contentStyle={{ background: "#141821", border: "1px solid #465064", borderRadius: 12 }} formatter={(value, name) => [Math.round(Number(value)), name]} /><Bar dataKey="load" fill="#465064" radius={[3, 3, 0, 0]} /><Area type="monotone" dataKey="fitness" stroke="#ffd23f" fill="url(#fitness)" strokeWidth={3} /><Line type="monotone" dataKey="fatigue" stroke="#ef4444" dot={false} strokeWidth={2} /><Line type="monotone" dataKey="form" stroke="#38bdf8" dot={false} strokeWidth={2} /></ComposedChart></ResponsiveContainer></div>; }
